import { AdminLoginView } from "@/modules/admin/views/AdminLoginView";

// Admin login is not a user-facing navigation target — no instant needed
export default function AdminLoginPage() {
  return <AdminLoginView />;
}
